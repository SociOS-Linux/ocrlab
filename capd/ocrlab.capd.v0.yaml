capability_id: "socio.ocrlab"
version: "0.1.0"
status: "draft"
description: "Linux-native capability repo for SourceOS: ocrlab"
entrypoints:
  validate:
    command: "make validate"
contracts:
  rpc_dir: "rpc"
  schema_dir: "schemas"
